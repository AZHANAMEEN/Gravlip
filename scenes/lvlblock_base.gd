extends Node2D
# Base script for level blocks
# Each level block should define its own BLOCK_WIDTH constant

const BLOCK_WIDTH: float = 592.0  # Override this in derived blocks

func _ready() -> void:
	pass
